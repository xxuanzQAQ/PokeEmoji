[project]
name = "PokeEmoji"
version = "1.0.0"
description = "戳一戳回复一张表情包，数据来自 emoji.wuwa.games 的公开接口。"
authors = [
    {name = "GsCore Plugin", email = ""},
]
dependencies = ["httpx", "msgspec", "Pillow"]
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}

[project.urls]
Repository = "https://github.com/xxuanzQAQ/PokeEmoji"
Source = "https://emoji.wuwa.games/"

[tool.pdm]
distribution = false
